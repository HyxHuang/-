<view class="project-wrap">
  <button class="addProject" bindtap="addProject">添加新项目</button>
  <block wx:for="{{projectList}}" wx:key="index">
    <project  projectMessage="{{item}}"></project>
  </block>
</view>
